import styled from 'styled-components';

export const SearchContainer = styled('div')`
  display: flex;
  align-items: center;

  label svg {
    color: #fff;
  }

  input {
    background: 0;
    border: 0;
    outline: 0;
    padding: 0.8rem;

    color: #ebedf5;
    font-size: 1rem;
    line-height: 1.3;
    width: 100%;
  }
`;
